# Claude Code CLI 使用文档

## 一、上下文与配额管理

### `/usage` 的提示含义
- `/usage` 会提示长会话（如 >150k context）占用比例
- 长会话成本高：即使有 prompt cache，token 越多每轮越贵
- 核心策略：**主动压缩或清理上下文**，不要让单次会话无限膨胀

### `/compact` vs `/clear`

| 命令 | 作用 | 使用场景 |
|------|------|----------|
| `/clear` | 完全清空上下文，从零开始 | 切换到完全无关的新任务 |
| `/compact` | 把历史压缩成摘要，保留任务进度 | 同一任务继续做，但上下文太长 |

- `/clear` = 失忆，从零开始
- `/compact` = 记笔记，扔掉原始记录
- `/compact` 后可能丢失细节，不要过早使用

### 会话恢复
- `claude --continue` — 恢复上一个会话
- `claude --resume` — 列出历史会话供选择
- 正常启动 `claude` 默认是新会话（空上下文）

---

## 二、省配额技巧

- **给具体路径**：不要让 Claude 自己搜索文件，直接给路径更便宜
- **拆分任务**：大任务分多个短会话做，避免单会话膨胀
- **用子 agent**（Task/Agent 工具）处理大量搜索，结果被总结后返回主会话，不污染主上下文
- **主动降档模型**：简单任务用 Haiku/Sonnet，复杂任务才用 Opus

---

## 三、交互快捷技巧

| 操作 | 说明 |
|------|------|
| `!<command>` | 输入框前加 `!` 直接执行 shell 命令（适合 `gcloud auth login` 等交互式登录） |
| `#<内容>` | 快速追加到 CLAUDE.md（项目记忆） |
| `@<文件路径>` | 直接引用文件，比描述更准确 |
| `Esc` | 打断 Claude 当前生成 |
| 双击 `Esc` | 回到之前某条消息重新开始（分支对话） |

---

## 四、配置文件

| 文件 | 作用 |
|------|------|
| `CLAUDE.md`（项目根目录） | 项目约定、常用命令、禁忌，每次自动加载 |
| `~/.claude/CLAUDE.md` | 全局个人偏好（如"回复用中文"、"别加 emoji"） |
| `.claude/settings.json` | 权限白名单，减少 "Allow?" 弹窗 |
| `/permissions` | 查看/编辑权限配置 |

---

## 五、常用斜杠命令

| 命令 | 作用 |
|------|------|
| `/help` | 所有命令列表 |
| `/cost` | 查看本次会话花费 |
| `/usage` | 查看配额使用情况 |
| `/model` | 切换模型（Opus/Sonnet/Haiku） |
| `/review` | 让 Claude 审查当前改动 |
| `/init` | 自动生成项目 CLAUDE.md |
| `/compact` | 压缩当前会话上下文 |
| `/clear` | 清空当前会话 |
| `/permissions` | 管理权限 |

---

## 六、模型选择策略

| 任务类型 | 推荐模型 |
|----------|----------|
| 简单任务（改变量名、小 bug） | Haiku / Sonnet |
| 中等任务（常规开发） | Sonnet |
| 复杂任务（架构、重构、难 debug） | Opus |

配额紧张时主动用 `/model` 降档。

---

## 七、工作流建议

1. **先规划再实现** — 复杂任务让 Claude 先给方案（Plan mode），确认后再写代码
2. **让它跑测试** — 改完代码让 Claude 自己跑测试/类型检查，形成验证闭环
3. **一次一件事** — 一个会话做一件相关的事，做完 `/clear`，不要混多个无关任务
4. **充分利用执行能力** — Claude Code 最强的是能实际执行（读代码、跑脚本、改文件），形成闭环而不是复制粘贴

---

## 八、安全避坑

- **别让 Claude 自动 push/merge** — 除非明确授权
- **重要改动先看 diff** — 不要无脑接受
- **Git 危险操作要看清** — `git reset --hard`、`git push --force` 等不可逆
- **不要跳过 hooks** — `--no-verify` 会绕过检查，有风险

---

## 九、一句话总结

**短会话 + 精准上下文 + 合适模型 + 主动清理** = 高效省钱的 Claude Code 使用方式。
