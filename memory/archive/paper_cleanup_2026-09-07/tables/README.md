# Generated tables

Place generated LaTeX table fragments in this directory and include them with
`\input{tables/<name>}`. Every generated table must record its source script
and input result directories in `../TASKS.md`.
